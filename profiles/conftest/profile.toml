[profile]
name        = "conftest"
description = "conftest holds the repository's configuration to its own policies, written and tested in Rego"
devset      = ">=0.1"

[files.".just/conftest.just"]

[files.".config/mise/conf.d/devset-conftest.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
