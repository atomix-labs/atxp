[profile]
name        = "rust-toolchain"
description = "The toolchain a checkout builds with: one pinned nightly, with miri, rustc-dev, llvm-tools and the sources"
devset      = ">=0.2"

[files."rust-toolchain.toml"]
scope = "keys"

[files.".just/rust-toolchain.just"]
