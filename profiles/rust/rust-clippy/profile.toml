[profile]
name        = "rust-clippy"
description = "Clippy over every crate, target and feature, warnings denied; tests may unwrap, panic and print"
devset      = ">=0.2.1"

[requires]
rust-toolchain = {}

[files."clippy.toml"]
scope = "keys"

[files.".just/rust-clippy.just"]
