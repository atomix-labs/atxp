[profile]
name        = "git-ignore"
description = "Git ignores what tools keep locally, editors' files, and anything that looks like a secret"
devset      = ">=0.3.0"

[files.".gitignore"]
scope   = "block"
starter = ".gitignore.starter"
