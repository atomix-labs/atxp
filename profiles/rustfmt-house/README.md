# `rustfmt-house`

The house rustfmt, a flavor of the collection's `rustfmt`: nightly's options,
with imports grouped by module, comments and doc examples wrapped at
`line_width`, and macros' matchers formatted. It needs a nightly rustfmt: take
`rust-nightly` with it.
