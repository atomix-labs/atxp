[profile]
name        = "cargo-publish"
description = "crates.io: every crate a release publishes packaged on each change, and published at the release by trusted publishing"
devset      = ">=0.2.2"

[requires]
just           = {}
rust-toolchain = {}

[files.".just/cargo-publish.just"]

[files.".just/cargo-publish.py"]
