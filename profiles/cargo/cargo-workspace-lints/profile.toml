[profile]
name        = "cargo-workspace-lints"
description = "Every crate in the workspace inherits its lints: `[lints] workspace = true`"
devset      = ">=0.2"

[files.".just/cargo-workspace-lints.just"]

[files.".config/mise/conf.d/devset-cargo-workspace-lints.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
