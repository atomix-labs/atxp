[profile]
name        = "cargo-binaries"
description = "A release's binaries: the workspace's, built for this machine, static on Linux, archived with a sha256"
devset      = ">=0.3.0"

[requires]
just           = {}
rust-toolchain = {}

[files.".just/cargo-binaries.just"]

[files.".just/cargo-binaries.sh"]
executable = true

[files.".gitignore"]
scope = "block"
