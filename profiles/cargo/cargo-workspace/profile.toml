[profile]
name        = "cargo-workspace"
description = "A Cargo workspace's own settings: its build output ignored, and requirements resolved within rust-version"
devset      = ">=0.2"

[files.".gitignore"]
scope = "block"

[files.".cargo/config.toml"]
scope = "keys"
