# `claude-skills`

The house's skills for Claude Code, in `.claude/skills/`: `writing-rustdoc`, the
house style for docs and comments with its audit, inventory and lint scripts;
and `writing-cargo-manifest`, the house shape of a `Cargo.toml`.
