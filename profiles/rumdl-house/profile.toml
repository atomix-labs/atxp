[profile]
name        = "rumdl-house"
description = "The house rumdl: the collection's rules, less those rustdoc links and code spans break, plus heading case"
devset      = ">=0.1"

[files.".rumdl.toml"]
scope  = "keys"
policy = "merge"

[files.".just/rumdl.just"]

[files.".config/mise/conf.d/devset-rumdl.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
