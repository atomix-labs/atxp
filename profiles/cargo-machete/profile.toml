[profile]
name        = "cargo-machete"
description = "cargo-machete finds dependencies no crate uses, fast, from the sources alone"
devset      = ">=0.1"

[files.".just/cargo-machete.just"]

[files.".config/mise/conf.d/devset-cargo-machete.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
