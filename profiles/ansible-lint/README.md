# `ansible-lint`

ansible-lint holds every playbook and role in `.ansible/` to the house's
`.ansible-lint`. It runs on the same ansible-core the house's boxes run,
installed beside it by uv. Pins both with mise.
