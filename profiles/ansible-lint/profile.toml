[profile]
name        = "ansible-lint"
description = "ansible-lint holds every playbook and role in .ansible/ to the house profile, on the pinned ansible-core"
devset      = ">=0.1"

[files.".ansible-lint"]

[files.".just/ansible-lint.just"]

[files.".config/mise/conf.d/devset-ansible-lint.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
