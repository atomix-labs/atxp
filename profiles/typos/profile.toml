[profile]
name        = "typos"
description = "typos checks the spelling of code, documents and configuration; a repository adds its own words"
devset      = ">=0.1"

[files."typos.toml"]
scope  = "keys"
policy = "merge"

[files.".just/typos.just"]

[files.".config/mise/conf.d/devset-typos.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
