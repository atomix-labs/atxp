[profile]
name        = "github-nightly"
description = "A nightly run of every `nightly-*` recipe, each a job of its own, watched"
devset      = ">=0.3.0"

[requires]
github-automation = {}
github-watch      = {}
just              = {}

[files.".github/workflows/nightly.yml"]
