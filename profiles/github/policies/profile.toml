[profile]
name        = "policies"
description = "Policies for GitHub Actions workflows, held by conftest: timeouts, stated permissions, no shadowed results"
devset      = ">=0.2"

[files."policy/workflows/workflows.rego"]

[files."policy/workflows/workflows_test.rego"]
