# `vscode-house`

The house VS Code settings, as keys of `.vscode/settings.json` beside those of
the collection's `vscode-rust`: formatting on paste, rumdl fixing on save,
Markdown links validated, and rust-analyzer seeing every feature, as clippy
does, in a target directory of its own so it never waits on a build.
