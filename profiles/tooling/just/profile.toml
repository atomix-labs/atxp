[profile]
name        = "just"
description = "The recipe spine: imports every active profile's recipes, and `check`, `fix` and the other verbs run them all"
devset      = ">=0.2.2"

[files."justfile"]
scope    = "block"
starter  = "justfile.starter"
template = true

[files.".config/mise/conf.d/devset-just.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
