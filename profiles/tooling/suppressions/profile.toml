[profile]
name        = "suppressions"
description = "Every lint suppression proves it still suppresses something, for the tools that do not report it themselves"
devset      = ">=0.3.0"

[files.".just/suppressions.just"]

[files.".just/suppressions.py"]
