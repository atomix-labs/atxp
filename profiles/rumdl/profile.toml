[profile]
name        = "rumdl"
description = "rumdl lints Markdown: line length left to the formatter, headings in title case"
devset      = ">=0.1"

[files.".rumdl.toml"]
scope  = "keys"
policy = "merge"

[files.".just/rumdl.just"]

[files.".config/mise/conf.d/devset-rumdl.toml"]

[files.".config/mise/mise.lock"]
scope = "keys"
